if(particle.body.position.x > 285 && particle.body.position.x < 520 && particle.body.position.y > 740){
    score = score + 100
    particle = null;
  }
  if(particle.body.position.x > 520 && particle.body.position.x < 795 && particle.body.position.y > 740){
    score = score + 200
    particle = null;
  }
